/*
  layMd.js
  注册Layui的组件
*/

layui.use(['layer', 'laypage', 'laydate', 'form', 'element', 'upload'], function() {
	var layer = layui.layer,
		laypage = layui.laypage,
		laydate = layui.laydate,
		form = layui.form(),
		element = layui.element();
	//使用组件

});
